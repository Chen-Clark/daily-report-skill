---
name: daily-report
slug: daily-report
displayName: "每日报工 · Daily Report"
version: "1.0.0"
agent_created: true
author: ck
summary: "每天把口述/粘贴的工作进展，整理成标准五块结构、工整偏正式、可直接复制到团队群的报工文案，并自动留档到腾讯文档。"
description: "当用户说「报工」「日报」「每日报工」「今日报工」「今天干了啥汇总」「daily report」或类似意图时使用。流程：用户口述或粘贴今天的工作（可零散、口语），AI 整理为标准五块（今日完成 / 明日计划 / 进行中 / 风险问题 / 需协调支持），语气工整偏正式，只标日期不写姓名；产出一段可直接复制到团队群（企业微信/钉钉/飞书）的文案，并自动留档到腾讯文档在线（团队可共享查看）。支持手动调用，也配套一个每日 18:00 的提醒自动化做双保险。"
tags: [报工, 日报, 团队同步, 周报, 效率, tencent-docs]
license: MIT
---

# 每日报工 · Daily Report

把一天的零散进展，变成群里人人能秒懂、腾讯文档里有迹可查的标准报工。

## 何时使用
- 用户说：报工 / 日报 / 每日报工 / 今日报工 / 今天干了啥汇总 / daily report。
- 用户想给团队群发一段当日工作同步，但不想自己排版。
- 用户想每天留一份工作记录，便于日后复盘、绩效佐证。

## 核心约束（锁定，不要自行改）
- **结构**：严格五块 —— 今日完成 / 明日计划 / 进行中 / 风险问题 / 需协调支持。
- **语气**：工整偏正式。用词规范、结构清晰，但不生硬、不堆砌。
- **署名**：只写日期，不写姓名/岗位（群里看得到是谁，文档按日期归档）。
- **交付**：产出「可直接复制」的纯文本文案（用户自己粘贴到群）；同时自动留档到腾讯文档。
- **不代发**：AI 不主动往群里发消息，发群动作由用户执行。

## 标准工作流
1. **收内容**：若用户已附上今天的进展（口述/粘贴/零散要点），直接用；若没有，用 1–2 句话引导用户口述，不要一次抛 5 个问题把人问烦。可先问「今天主要完成了哪几件事？」，拿到后再补全其余块。
2. **整理成五块**：把零散信息归位到五个板块。规则：
   - 今日完成：今天已交付/关闭的事项，能带量化尽量带（如「完成 3 个接口联调」）。
   - 明日计划：明确、可验收的下一步，不超过 5 条。
   - 进行中：仍在推进、未关闭的事项及当前进度百分比（若可知）。
   - 风险问题：可能延期/受阻的点，点明影响与原因。
   - 需协调支持：需要谁、在什么时间点提供什么，越具体越好。
3. **产出文案**：用下方模板渲染。整体控制在「群里扫一眼能看完」的体量。
4. **展示给用户**：把文案放在代码块里，附一句「复制下面这段发到群里即可」。
5. **自动留档**：见下方「留档」章节。

## 文案模板
```
每日报工 · 2026-08-28（周五）

【今日完成】
1. xxx
2. xxx

【明日计划】
1. xxx
2. xxx

【进行中】
- xxx（进度约 70%）

【风险问题】
- xxx（影响：xxx；原因：xxx）

【需协调支持】
- 需要 @某某 在 x 前提供 xxx
```
- 日期格式：`YYYY-MM-DD（周X）`，周X 由日期推算。
- 标题用「每日报工 · 日期」，板块标题用【】包裹，保持工整。
- 若某块当天无内容，写「无」以保证五块完整、便于长期对比（也可省略，但建议保留「无」）。

## 留档（腾讯文档在线）
目标：团队可共享查看的历史归档。

1. **优先用腾讯文档连接器**（个人版 `tencent-docs` 或企业版 `tencent-saas-docs`）：
   - 首次运行：新建一篇文档，标题 `每日报工归档`，把本次五块内容（含日期标题）写入；将返回的 `file_id` 记录到本技能目录下的 `state.json`（`{ "archive_doc_id": "xxx" }`）。
   - 后续运行：用记录的 `file_id` 打开该文档，在文末追加本次内容（日期标题 + 五块）。
   - 若连接器未连接，先给出文案，再明确提示用户去「连接器」里开启腾讯文档，开启后重跑一次即可补档。
2. **降级兜底**：若用户暂不打算连腾讯文档，或连接器调用失败，把同样内容存为本地 Markdown `每日报工-YYYY-MM-DD.md`（目录可配，默认 `D:\workspace\每日报工\`），并提示「已存本地，连上腾讯文档后可一键迁移」。
3. 留档完成后，回用户一句：「已存档至腾讯文档《每日报工归档》✅」（或给出本地路径）。

## 配套提醒（双保险）
- 提供一个每日 18:00 的提醒自动化：到点轻提醒用户「该报工啦」，用户口述后调用本 skill。
- 自动化 prompt 示例：「提醒用户做今日报工：调用 daily-report 技能，请用户口述今天的工作进展，整理成五块文案并留档。」
- 用户也可纯手动调用，不依赖提醒。

## 边界与禁忌
- 信息不足时先问用户，不要编造「今日完成」的内容。
- 不擅自往任何群/文档发消息，除非用户明确要求且连接器已授权。
- 不写姓名（除非用户当天主动要求）。
- 量化数据如果用户没给，不要硬编；可留空或写「（待补充）」。

## 配置项（可选，写在 state.json 或用户口头指定）
- `archive_doc_id`：腾讯文档归档文档 id（首次自动写入）。
- `local_archive_dir`：本地兜底目录，默认 `D:\workspace\每日报工\`。
- `reminder_time`：提醒时间，默认 18:00。
